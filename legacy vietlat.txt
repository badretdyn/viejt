import re

user_input = input()

def make_upper_list(strng):
    
    my_list = []
    my_list = list("hello")
    squares = [x**2 for x in range(5)] # 0 1 4 9 16
    
    my_list.append("world!")
    my_list.insert(5, " ")
    
    my_list.extend(" everyone!")
    
    excl_mark = my_list.pop()
    my_list.insert(0, excl_mark)
    
    my_list.remove("o")
    
    is_excl_mark_in = str("!" in my_list)
    print(is_excl_mark_in)
    my_list.extend(is_excl_mark_in)
    
    del my_list[-1]
    #index(str, a, b) count(str)
    #sort(reverse = True) reverse()
    
    streng = "".join(my_list)
    
    return my_list #streng

def translate(str):
    
    result = str
    
    result = result.replace("d", "z")
    result = result.replace("đ", "d")
    
    result = result.replace("y", "i")
    
    result = result.replace("e", "ae")
    result = result.replace("ê", "e")
    
    result = result.replace("ư", "y")
    
    result = result.replace("ơ", "w")
    result = result.replace("ô", "ou")
    
    result = result.replace("â", "oe")
    
    # for i in range(len(result) - 1, -1, -1):
        # if result[i] == 'ă':
            # if i + 1 < len(result) and result[i + 1].isalpha() and result[i + 1].isascii():
                # result = result[:i+1] + result[i+1] + result[i+1:]
    
    result = re.sub(r'ă([a-zA-Z])', r'ă\1\1', result)
    
    result = result.replace("ă", "a")
    
    return result



def main():
    print(translate("dăn kă băn\n\nnước\ngiới\ngiữa\nViệt\nNhật Bản\nTrung Quốc\nThổ Nhĩ Kỳ\nU-crai-na\nmúa\nkẹo\nTô-ky-ô\nBắc Kinh\ncâu chuyện\nDim-ba-bu-ê\nĐan Mạch\nghi chép\ntên tôi\ndươi"))
    print(translate(user_input))
    print(make_upper_list(""))
    

if __name__ == "__main__":
    main()